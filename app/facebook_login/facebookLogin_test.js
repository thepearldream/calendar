'use strict';

describe('myApp.facebookLogin module', function() {

  beforeEach(module('myApp.facebookLogin'));

  describe('facebookLogin controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var facebookLoginCtrl = $controller('FacebookLoginCtrl');
      expect(facebookLoginCtrl).toBeDefined();
    }));

  });
});